# Frontend (Vite) — only VITE_* vars are exposed to the browser. Do not put API secrets here.

# API base URL for the SkilledClaws backend. Leave empty to use Vite proxy (recommended in dev).
# For a hosted API, set to the full root URL (e.g. https://your-api.example.com).
VITE_API_URL=

# Optional: use Supabase from the frontend (e.g. past skills). Anon key only — never put service_role here.
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
